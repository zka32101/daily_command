# Daily Command — AIアドバイス機能 & ユニット拡張 設計書 v2.0

**作成日**: 2026-07-02
**対象バージョン**: v2.0
**前提**: AIアドバイスは Firebase 本設定不要（クライアント側API呼び出しのみ）で先行実装可能。ユニット拡張はモデル拡張のみで着手可能（Firebase不要）。

---

# Part A: AIアドバイス機能

## 1. Vision

```
敗北画面の「あと少しで勝てた…」を、もう一段具体的にする。
「なんとなく強化」ではなく「どのユニットの何を強化すべきか」をAIが一言で示す。
```

**既存の StageDefeatScreen との違い**:
- v1.0: 「ユニットを強化すれば次は勝てる」（固定文言・全員共通）
- v2.0: 「アーチャーの攻撃力が足りません。Red鉱石を優先装備しましょう」（対戦データに基づく個別提案）

## 2. なぜAIが必要か（ルールベースで十分では？）

実は最初の判断として検討すべき：**単純なif分岐で足りるなら、AI API呼び出しは過剰**。

```
判定ロジック（ルールベースで実装可能）:
- 敗北時のダメージログを見て、被弾量が一番多かった属性 → 弱点
- 装備鉱石が不足している属性 → 推奨鉱石

これは if 文で書ける。API 呼び出し不要でレイテンシもコストもゼロ。
```

→ **結論: 「弱点分析」自体はルールベースで実装し、AIは「言い回しのバリエーション」にのみ使う。**
これにより:
- Haiku（安価・低レイテンシ）で「同じ弱点情報」を毎回違う励まし文に変換するだけの軽いタスクに限定
- API障害時はルールベースの固定文言にフォールバック可能（可用性を損なわない）

## 3. 技術設計

```
【弱点分析ロジック（クライアント側・ルールベース）】
入力: 直近のステージ敗北時の戦闘ログ（被ダメージ内訳、残りHP、経過時間）
出力: WeaknessAnalysis {
  weakestStat: string       // "attack" | "defense" | "health"
  recommendedMineral: string // "Red" | "Green" | "Blue"
  recommendedUnitId: string  // どのユニットを強化すべきか
}

判定例:
- 制限時間切れで敗北（120秒到達）→ attack不足 → Red推奨
- HPが早く0になった → defense/health不足 → Blue/Green推奨
- 特定ユニットのレベルが他より低い → そのユニットを優先育成
```

```
【AIコメント生成（Haiku API・オプショナル）】
入力: WeaknessAnalysis + プレイヤーの連続敗北回数（励まし度合いの調整用）
出力: 1文の日本語コメント（例: "アーチャーの火力が伸び悩んでいますね。
       Red鉱石を集めて装備すれば、次は突破できるはずです！"）

呼び出しタイミング: 敗北画面表示時のみ（1日あたり最大 敗北回数分 = 数回程度）
コスト試算: Haiku 入力200token/出力100token 程度 × 想定 1日3-5回/ユーザー
          → 月間コストは既存プロジェクト（ことばえ等）と同水準の 数百円〜千円台/月 に収まる規模
フォールバック: API失敗・タイムアウト時は WeaknessAnalysis から
              固定テンプレート文言を生成（可用性維持、ユーザー体験を止めない）
```

## 4. UI 統合（既存 StageDefeatScreen 拡張）

```
既存の「推奨フロー」パネル（GlassPanel）の上に
AIコメントを1行追加するのみ。新規画面は不要。

┌───────────────────────────┐
│ 🤖 「アーチャーの火力が足りません。  │  ← 新規: AIコメント欄
│    Red鉱石を集めましょう」          │
├───────────────────────────┤
│ 推奨フロー（既存）                  │
│ 1. 採掘で鉱石を獲得                 │
│ 2. ユニットを強化                   │
│ 3. ステージに再挑戦                  │
└───────────────────────────┘
```

## 5. KPI 計測

```
ai_advice_shown       — AIコメント表示（API成功/フォールバックのフラグ付き）
ai_advice_followed    — 提案された鉱石属性を実際に装備したか（提案の有効性検証）
```

## 6. 実装フェーズ（推定工数）

```
Phase 1: 弱点分析ロジック（ルールベース、API不要）        [3-4h]
  - 戦闘ログから WeaknessAnalysis を算出する純粋関数
  - ユニットテストで判定ロジックを検証（app-dev-methodology 準拠）

Phase 2: Haiku API 連携 + フォールバック                  [3-4h]
  - Claude API（Haiku）呼び出しラッパー
  - タイムアウト/失敗時のテンプレート文言フォールバック
  - APIキーは環境変数管理（Cloud Functions経由が望ましいが、
    暫定的にクライアント直呼びも可、ただしAPIキー露出リスクに注意）

Phase 3: UI統合 + KPI計測                                [2h]

合計: 8-10h（Firebase不要、最も着手しやすいv2.0機能）
```

**セキュリティ注意**: APIキーをクライアントに直接埋め込むと悪用されるリスクがある。
理想は Cloud Functions 経由（Firebase必要）。Firebase未整備の間は
「弱点分析のみ実装し、AIコメント生成は Firebase 本設定後に追加」の2段階リリースを推奨。

---

# Part B: ユニット拡張（3体 → 6+体）

## 1. Vision

```
Warrior / Mage / Archer の3体固定から、デッキ編成の戦略性を上げる。
「毎回同じ3体」ではなく「5体から3体を選ぶ」ようにし、デッキ編集画面の存在意義を作る
（現状 home_screen.dart の「デッキ編集」ボタンは未実装のダミー）。
```

## 2. 追加ユニット案（3種）

既存3体（Warrior/Mage/Archer）に加えて：

```yaml
Tank（盾役）:
  役割: 高Defense・低Attack。前衛で耐える
  属性補正: Blue鉱石特化（getDefense() の補正を厚めに）

Healer（回復役）:
  役割: 戦闘中にプレイヤーHPを回復する特殊アクション追加
  実装難度: 中（StageScreenの_attack()にHeal分岐が必要）

Assassin（速攻役）:
  役割: 低HP・高Attack。クリティカル率upなど既存コンボシステムと相性良い
  属性補正: Red鉱石特化 + stage_screen.dart の _comboCount ボーナスとシナジー
```

**優先順位**: Tank → Assassin → Healer
（Healerは戦闘ロジックの変更が必要で最もコストが高いため後回し）

## 3. データモデルへの影響

既存の `Unit` モデル（`lib/models/unit.dart`）は `unitType: String` で既に汎用設計済み。
**モデル変更は不要**、以下のみ追加：

```dart
// player_provider.dart の初期ユニット配列に追加するだけ
Unit(id: 'tank_1', unitType: 'Tank', ...)
Unit(id: 'assassin_1', unitType: 'Assassin', ...)
```

`unit.dart` の `getAttack()` / `getDefense()` / `getHealth()` は属性補正ロジックが
すでに Red/Green/Blue 汎用なので、ユニットタイプ別の基礎値テーブルを追加するだけで拡張できる：

```dart
// 現状: 全ユニット共通の計算式（level * 5 など）
// 拡張: unitType 別の基礎倍率テーブルを追加
static const Map<String, double> _attackMultiplier = {
  'Warrior': 1.0, 'Mage': 0.8, 'Archer': 1.1,
  'Tank': 0.6, 'Assassin': 1.4, 'Healer': 0.5,
};
```

## 4. デッキ編集画面（新規実装）

```
home_screen.dart の「デッキ編集」ボタン（現在ダミー）から遷移

┌─────────────────────────────────┐
│ デッキ編集画面                     │
│  ├─ 所有ユニット一覧（6体グリッド）  │
│  ├─ 選択中デッキスロット（3〜5体）   │
│  ├─ ドラッグ&ドロップ配置           │
│  └─ [保存]                        │
└─────────────────────────────────┘
```

既存の SkinSelectionScreen のグリッドカードパターン（押下バウンス+グロー枠）を流用可能。

## 5. 実装フェーズ（推定工数）

```
Phase 1: Tank + Assassin ユニット追加（データのみ）         [2-3h]
  - unit.dart に基礎倍率テーブル追加
  - player_provider.dart 初期データ拡張

Phase 2: デッキ編集画面 UI                                 [5-6h]
  - グリッド選択 + スロット配置UI
  - 既存 SkinSelectionScreen パターン流用

Phase 3: Healer 特殊アクション（戦闘ロジック変更）          [4-5h]
  - StageScreen に「回復」ボタン追加、_attack()と並列の_heal()実装
  - クールタイム制（連打回復の抑制）

合計: 11-14h（Firebase不要、AIアドバイスと並行実装可能）
```

---

## ✅ 設計完了チェック

```
□ AIアドバイス: ルールベース判定 + AI(Haiku)は言い回し生成のみに限定する設計判断を明記
□ AIアドバイス: API障害時のフォールバック（可用性維持）を明記
□ AIアドバイス: APIキー露出リスクとFirebase経由が理想である旨を明記
□ ユニット拡張: 既存モデル（unit.dart）が変更不要な汎用設計であることを確認
□ ユニット拡張: 優先順位（Tank→Assassin→Healer）と理由を明記
□ デッキ編集画面: 既存UIパターン（SkinSelectionScreen）流用方針を明記
□ 両機能とも Firebase 本設定不要で着手可能（PvP/クランより着手しやすい）
□ 実装フェーズ・工数見積もり完了（AIアドバイス 8-10h、ユニット拡張 11-14h）
→ すぐに実装開始可能（他のv2.0機能と異なりブロッカーなし）
```
